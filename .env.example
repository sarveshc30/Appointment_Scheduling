# Example environment variables for local development
# Frontend: base URL for the Flask API (used by Vite via import.meta.env)
VITE_API_BASE_URL=http://localhost:5000

# Backend: CORS allowed origins (comma-separated). Use Vite dev origin for local dev.
CORS_ORIGINS=http://localhost:5173

# Other backend envs (db, supabase, twilio) — keep your existing keys
# SUPABASE_URL=
# SUPABASE_KEY=
# account_sid=
# auth_token=
